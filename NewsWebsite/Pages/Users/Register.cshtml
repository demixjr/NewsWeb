@page
@model NewsWebsite.Pages.Users.RegisterModel
@{
    ViewData["Title"] = "Реєстрація";
}

<div class="auth-wrap">
    <div class="auth-title">Реєстрація</div>
    <div class="auth-subtitle">Оберіть роль у редакції</div>

    <form method="post">
        <div class="form-group">
            <label asp-for="Input.Username" class="form-label" for="Input_Username">Ім'я користувача</label>
            <input asp-for="Input.Username" class="form-control" autofocus id="Input_Username" />
            <span asp-validation-for="Input.Username" class="form-error"></span>
        </div>

        <div class="form-group">
            <label asp-for="Input.Password" class="form-label" for="Input_Password">Пароль</label>
            <input asp-for="Input.Password" class="form-control" id="Input_Password" />
            <span asp-validation-for="Input.Password" class="form-error"></span>
        </div>

        <div class="form-group">
            <label asp-for="Input.ConfirmPassword" class="form-label" for="Input_ConfirmPassword">Підтвердіть пароль</label>
            <input asp-for="Input.ConfirmPassword" class="form-control" id="Input_ConfirmPassword" />
            <span asp-validation-for="Input.ConfirmPassword" class="form-error"></span>
        </div>

        <div class="form-group">
            <label asp-for="Input.Role" class="form-label" for="Input_Role">Роль</label>
            <select asp-for="Input.Role" class="form-control" id="Input_Role">
                <option value="">— Оберіть роль —</option>
                <option value="Writer">Writer — публікація статей</option>
                <option value="Admin">Admin — повний доступ</option>
            </select>
            <span asp-validation-for="Input.Role" class="form-error"></span>
        </div>

        <div class="form-actions">
            <button type="submit" class="btn btn-primary">Зареєструватись</button>
        </div>
    </form>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}